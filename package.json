{
  "name": "mooligan",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp check && vp run -r test && vp run -r build",
    "dev": "vp run --parallel --filter ./apps/desktop --filter ./apps/api dev",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@oxlint/plugins": "catalog:",
    "eslint-plugin-react-you-might-not-need-an-effect": "catalog:",
    "oxlint": "catalog:",
    "vite": "catalog:",
    "vite-plus": "catalog:"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.17.0",
      "onFail": "download"
    }
  },
  "engines": {
    "node": ">=22.18.0"
  }
}
